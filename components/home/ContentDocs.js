const ContentDocs = () => {
  return (
    <iframe
      src="https://docs.decentral.games/"
      style={{
        position: 'absolute',
        paddingTop: '0px',
        height: '100%',
        width: '100%',
        border: 'none',
        overflow: 'hidden',
      }}
    />
  );
};

export default ContentDocs;
